В то время пока наша файловая система предоставляет полный доступ к байтам, хранящимся на диске,
нам нужен способ вставлять байты по середине или обрезать информацию из любого места в
файле. Согласно стандарту POSIX мы добавим insert
и truncate функции.

